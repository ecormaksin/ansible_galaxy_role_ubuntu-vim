---
# vars file for ansible_galaxy_role_ubuntu-vim

etc:
  profile:
    file:
      path: /etc/profile

vim:
  rc:
    file:
      path: /etc/vim/vimrc
  rc_ext:
    dist:
      file:
        path: /etc/vim/vimrc.local
